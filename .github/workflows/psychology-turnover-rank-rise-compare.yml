name: Psychology turnover rank rise compare

on:
  push:
    branches:
      - research/crowd-psychology-3y-backtest
    paths:
      - 'momentum5d/scripts/compare_psychology_turnover_rank_rise.py'
      - '.github/workflows/psychology-turnover-rank-rise-compare.yml'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy
      - name: Run comparison
        run: |
          mkdir -p research-output
          python momentum5d/scripts/compare_psychology_turnover_rank_rise.py \
            --data-dir dashboard-data/technical-backtest-3y \
            --output-json research-output/psychology-turnover-rank-rise-compare.json \
            --output-md research-output/psychology-turnover-rank-rise-compare.md
      - name: Show summary
        run: cat research-output/psychology-turnover-rank-rise-compare.md
      - uses: actions/upload-artifact@v4
        with:
          name: psychology-turnover-rank-rise-compare
          path: research-output/
